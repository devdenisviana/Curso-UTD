// Define um objeto "env" que contém as configurações de ambiente para se conectar ao banco de dados
const env = {
    database: 'db_projeto', // Nome do banco de dados
    username: 'root', // Nome de usuário do banco de dados
    password: 'Gaia', // Senha do banco de dados
    host: 'localhost', // Host do banco de dados
    dialect: 'mysql', // Dialeto do banco de dados (no caso, MySQL)
};
